[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "madkit"
version = "0.1.1"
description = "Multi-Agent Development Kit — CLI para scaffolding multi-IDE de proyectos asistidos por agentes."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
authors = [{ name = "Guillermo Montero" }]
keywords = ["claude-code", "cursor", "codex", "agents", "templates", "scaffold", "ai-coding", "mad"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Code Generators",
]
dependencies = [
    "typer>=0.12",
    "rich>=13",
    "platformdirs>=4",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pytest-cov>=5",
    "ruff>=0.6",
    "build>=1.2",
]

[project.scripts]
madkit = "madkit.cli:app"

[project.urls]
Homepage = "https://github.com/multi-agent-development-kit/madkit"
Repository = "https://github.com/multi-agent-development-kit/madkit"
Issues = "https://github.com/multi-agent-development-kit/madkit/issues"
Discussions = "https://github.com/multi-agent-development-kit/madkit/discussions"

[tool.hatch.build.targets.wheel]
packages = ["src/madkit"]

[tool.hatch.build.targets.wheel.force-include]
"src/madkit/templates" = "madkit/templates"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers"
markers = [
    "slow: tests más lentos (build de wheel, e2e). Skip con `-m 'not slow'`.",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
